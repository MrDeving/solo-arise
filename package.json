{
  "name": "Arise-solo",
  "version": "1.0.0",
  "description": "Solo Leveling System Task Manager",
  "main": "www/index.html",
  "scripts": {
    "build": "echo 'Standard web app, no build step required'",
    "sync": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}